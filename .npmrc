# Disable use of symlinks for cross-device installation compatibility
node-linker=hoisted

# Force PNPM to use copy instead of links
use-node-version=18.x

# Vercel performance optimizations
prefer-frozen-lockfile=true
